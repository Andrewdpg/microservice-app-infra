apiVersion: v1
kind: Service
metadata:
  name: web-frontend
  namespace: microservices-staging
spec:
  selector:
    app: web-frontend
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 31080
    protocol: TCP
  type: NodePort
